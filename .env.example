# AirResolve Environment Configuration Example
# (Note: AirResolve runs fully locally with a deterministic Python policy engine by default)

# Server Configuration
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_HEADLESS=true

# Optional External Model Integration (Disabled by default for strict deterministic operation)
# OPENAI_API_KEY=your_optional_openai_api_key_here
# MODEL_NAME=gpt-4o-mini
